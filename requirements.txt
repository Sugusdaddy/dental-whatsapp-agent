# Agente Dental — dependencias

# IA
langgraph>=0.2.0
langchain-anthropic>=0.3.0
langchain-core>=0.3.0

# API
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.28.0

# Scheduler
apscheduler>=3.10.0

# Base de datos (producción)
asyncpg>=0.30.0       # PostgreSQL async
redis>=5.2.0          # memoria conversaciones + cola

# Utils
pydantic>=2.0.0
python-dotenv>=1.0.0

# Tests
pytest>=8.0.0
pytest-asyncio>=0.24.0
